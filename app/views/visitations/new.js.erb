<%= "$('local_status_#{@thread_id}').update('Testing sites #{@offset + 1} through #{@batch_size+@offset}...');"  %>
<%= remote_function :url => visitations_url(:thread_id => @thread_id ), :method => :post, 
		:with => "'results=' + JSON.stringify(CSSHistory.check_batch(#{@sites.inject({}){|m,s| m[s.url] = s.id; m }.to_json}, '#{Rails.cache.read "scraping_#{session[:scraping_id]}_method"}'))"
%>
